import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'post-exercise',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
